# AVF Debian rootless container storage.
# The VM-local home is disposable; use fast rootless overlay storage here.
[storage]
driver = "overlay"

[storage.options.overlay]
mount_program = "/usr/bin/fuse-overlayfs"
